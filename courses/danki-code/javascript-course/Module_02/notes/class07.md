# Aula 07 - Variáveis e Constantes

```jsx
// VARIÁVEIS (VAR / LET / CONST)

// VAR - GLOBAL
var firstName = 'César';
lastName = 'Canoff';
console.log(name);
// -----------------

// LET - ESCOPE
let age = 17;
console.log(age);
// -----------------

// CONST - NO CHANGES
const valuePI = 3.1415;
console.log(valuePI);
// -----------------

let number = 55; // NUMBER
let text = 'House'; // STRING
let isPerson = true; // BOOOLEAN

// Concatenate two values (String)
console.log('Hello ' + firstName + ' ' + lastName);
```